<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal do aplicativo -->
    <style name="Theme.PlantGuide" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryVariant">@color/green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/green_accent</item>
        <item name="colorSecondaryVariant">@color/green_light</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/green_dark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!-- Tema da Splash Screen -->
    <style name="Theme.PlantGuide.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_plant_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.PlantGuide</item>
    </style>

    <!-- AppBar -->
    <style name="Theme.PlantGuide.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    <style name="Theme.PlantGuide.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

</resources>